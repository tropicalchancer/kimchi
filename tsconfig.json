{
  "compilerOptions": {
    /* ───────── existing settings ───────── */
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,

    /* next-js plugin */
    "plugins": [{ "name": "next" }],

    /* path alias */
    "paths": {
      "@/*": ["./src/*"]
    },

    /* ───────── added line (fixes Vercel build) ───────── */
    "types": ["@types/styled-components", "node"]
  },

  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    "src/types/**/*.d.ts"
  ],
  "exclude": ["node_modules"]
}
